{
    "name": "api-architecture-diagram",
    "type": "module",
    "version": "1.0.0",
    "scripts": {
        "dev": "npm run build-scripts && astro dev",
        "build": "npm run build-scripts && astro check && astro build",
        "preview": "astro preview",
        "check": "astro check",
        "type-check": "tsc --noEmit",
        "build-scripts": "tsc --project tsconfig.scripts.json",
        "test": "vitest",
        "test:run": "vitest run"
    },
    "dependencies": {
        "@astrojs/tailwind": "^5.0.0",
        "astro": "^4.0.0",
        "autoprefixer": "^10.4.16",
        "tailwindcss": "^3.3.0"
    },
    "devDependencies": {
        "@types/node": "^20.0.0",
        "jsdom": "^23.2.0",
        "typescript": "^5.3.0",
        "vitest": "^1.6.1"
    }
}
