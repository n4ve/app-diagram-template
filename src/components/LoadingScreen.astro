---
// Loading screen component
---

<div id="loadingScreen" class="fixed inset-0 bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center z-50">
  <div class="bg-white rounded-2xl p-8 shadow-2xl text-center max-w-md mx-4">
    <!-- Loading Animation -->
    <div class="relative mb-6">
      <div class="animate-spin rounded-full h-16 w-16 border-4 border-gray-200 border-t-blue-500 mx-auto"></div>
      <div class="absolute inset-0 flex items-center justify-center">
        <div class="text-2xl">🏗️</div>
      </div>
    </div>
    
    <!-- Loading Text -->
    <h2 class="text-2xl font-bold text-gray-800 mb-3">กำลังโหลดข้อมูล...</h2>
    <p class="text-gray-600 mb-6">โปรดรอสักครู่ระหว่างที่เราเตรียม API Architecture Diagram</p>
    
    <!-- Progress Indicators -->
    <div class="space-y-3">
      <div class="flex items-center justify-between text-sm text-gray-500">
        <span class="flex items-center gap-2">
          <span>📱</span>
          <span>Loading Pages...</span>
        </span>
        <div class="w-20 h-2 bg-gray-200 rounded-full overflow-hidden">
          <div class="h-full bg-blue-500 rounded-full animate-pulse loading-bar" style="animation-delay: 0s;"></div>
        </div>
      </div>
      
      <div class="flex items-center justify-between text-sm text-gray-500">
        <span class="flex items-center gap-2">
          <span>🖥️</span>
          <span>Loading Servers...</span>
        </span>
        <div class="w-20 h-2 bg-gray-200 rounded-full overflow-hidden">
          <div class="h-full bg-green-500 rounded-full animate-pulse loading-bar" style="animation-delay: 0.2s;"></div>
        </div>
      </div>
      
      <div class="flex items-center justify-between text-sm text-gray-500">
        <span class="flex items-center gap-2">
          <span>📸</span>
          <span>Loading Screenshots...</span>
        </span>
        <div class="w-20 h-2 bg-gray-200 rounded-full overflow-hidden">
          <div class="h-full bg-orange-500 rounded-full animate-pulse loading-bar" style="animation-delay: 0.4s;"></div>
        </div>
      </div>
      
      <div class="flex items-center justify-between text-sm text-gray-500">
        <span class="flex items-center gap-2">
          <span>⚡</span>
          <span>Initializing...</span>
        </span>
        <div class="w-20 h-2 bg-gray-200 rounded-full overflow-hidden">
          <div class="h-full bg-purple-500 rounded-full animate-pulse loading-bar" style="animation-delay: 0.6s;"></div>
        </div>
      </div>
    </div>
    
    <!-- Fun Loading Messages -->
    <div class="mt-6 text-xs text-gray-400 animate-pulse">
      <div id="loadingMessage">กำลังเตรียมข้อมูล API...</div>
    </div>
  </div>
</div>

<style>
  .loading-bar {
    width: 0%;
    animation: loading-progress 2s ease-in-out forwards;
  }
  
  @keyframes loading-progress {
    0% { width: 0%; }
    100% { width: 100%; }
  }
  
  /* Loading message rotation */
  #loadingMessage {
    min-height: 1rem;
  }
</style>

<script>
  // Rotating loading messages
  const messages = [
    'กำลังเตรียมข้อมูล API...',
    'กำลังวิเคราะห์โครงสร้าง...',
    'กำลังสร้างแผนผัง...',
    'เกือบเสร็จแล้ว...'
  ];
  
  let messageIndex = 0;
  const messageElement = document.getElementById('loadingMessage');
  
  if (messageElement) {
    const rotateMessages = () => {
      messageIndex = (messageIndex + 1) % messages.length;
      messageElement.textContent = messages[messageIndex];
    };
    
    // Rotate messages every 500ms
    const messageInterval = setInterval(rotateMessages, 500);
    
    // Clear interval when loading is done
    setTimeout(() => {
      clearInterval(messageInterval);
    }, 2000);
  }
</script>